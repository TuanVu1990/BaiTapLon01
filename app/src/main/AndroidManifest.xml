<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="tutorial.freelancertraning.tuanvu.baitaplon01">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LoginActivity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Menuactivity.MenuActivity" />
        <activity android:name=".SearchActivity.SearchActivity" />
        <activity android:name=".ActualiesActivity.ActualitesActivity" />
        <activity android:name=".AgendarActivity.ProfileActivity" />
        <activity android:name=".Membres.MembresActivity" />
        <activity android:name=".AdvantagesActivity.AdvantagesActivity"></activity>
    </application>

</manifest>